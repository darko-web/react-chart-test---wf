import * as React from "react";
import * as Types from "./types";

declare function HeroTextDark(props: {
  as?: React.ElementType;
  heroTitle?: React.ReactNode;
  heroIntro?: React.ReactNode;
}): React.JSX.Element;
