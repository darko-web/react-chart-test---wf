import * as React from "react";
import * as Types from "./types";

declare function NavigationMenu(props: {
  as?: React.ElementType;
  navigationMenuAdressDetails?: React.ReactNode;
}): React.JSX.Element;
