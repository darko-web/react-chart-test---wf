import * as React from "react";
import * as Types from "./types";

declare function LayoutCardRightLight(props: {
  as?: React.ElementType;
  cardImage?: Types.Asset.Image;
  cardHeading?: React.ReactNode;
  cardText?: React.ReactNode;
}): React.JSX.Element;
